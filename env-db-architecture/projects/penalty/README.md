# Proyecto Penalty

Gestión de PostgreSQL con ambientes (`dev`, `qa`, `staging`, `prod`) y backups independientes.

## Uso Rápido
```powershell
./init-project.ps1 -ProjectName "penalty"
./sync-config.ps1
./manage.ps1 dev up
```

Más detalles y diagramas en la raíz (`ARCHITECTURE.md`).
